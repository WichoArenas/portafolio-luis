---
import Layout from '../../layouts/Layout.astro';
---

<Layout>
  <canvas id="neural-bg" class="fixed top-0 left-0 w-full h-full z-[-1] opacity-20 pointer-events-none"></canvas>

  <nav class="max-w-6xl mx-auto px-6 py-8 flex flex-wrap gap-4 justify-between items-center relative z-10">
    <a href="/portafolio-luis/en/" class="text-sm font-bold tracking-[0.25em] text-slate-200 hover:text-cyan-300 transition-colors">
      LUIS_ARENAS<span class="text-cyan-400">.DEV</span>
    </a>

    <div class="flex flex-wrap gap-3 items-center text-sm">
      <a href="#research" class="text-slate-400 hover:text-cyan-300 transition-colors">Research</a>
      <a href="#projects" class="text-slate-400 hover:text-cyan-300 transition-colors">Projects</a>
      <a href="#publications" class="text-slate-400 hover:text-cyan-300 transition-colors">Publications</a>
      <a href="#contact" class="text-slate-400 hover:text-cyan-300 transition-colors">Contact</a>
      <a href="/portafolio-luis/" class="ml-2 text-xs font-semibold text-slate-300 hover:text-white border border-slate-700 px-3 py-1.5 rounded-md transition-colors">
        ES / EN
      </a>
    </div>
  </nav>

  <main class="max-w-6xl mx-auto px-6 py-10 relative z-10">
    <header class="grid grid-cols-1 lg:grid-cols-[1.25fr_0.75fr] gap-10 items-center">
      <section class="space-y-6">
        <div class="flex flex-wrap gap-3">
          <span class="inline-flex px-3 py-1 rounded-full border border-blue-500/30 bg-blue-500/10 text-blue-300 text-sm font-medium tracking-wide">
            Research Stay in Canada
          </span>
          <span class="inline-flex px-3 py-1 rounded-full border border-purple-500/30 bg-purple-500/10 text-purple-300 text-sm font-medium tracking-wide">
            AI · Vision · Embedded Systems
          </span>
        </div>

        <h1 class="text-5xl md:text-7xl font-bold tracking-tight text-white">
          Hi, I'm <span class="text-transparent bg-clip-text bg-gradient-to-r from-cyan-400 via-blue-500 to-purple-500">Luis Arenas</span>
        </h1>

        <p class="text-xl md:text-2xl text-slate-400 max-w-4xl leading-relaxed">
          PhD researcher and technology developer working at the intersection of
          <strong class="text-slate-100">computer vision</strong>,
          <strong class="text-slate-100">artificial intelligence</strong>,
          <strong class="text-slate-100">multimodal emotion recognition</strong>,
          and <strong class="text-slate-100">real-time embedded systems</strong>.
        </p>

        <div class="flex flex-wrap gap-3 pt-2">
          <a href="#publications" class="px-5 py-3 rounded-xl bg-cyan-500 text-slate-950 font-bold hover:bg-cyan-300 transition-all shadow-[0_0_25px_rgba(6,182,212,0.25)]">
            View Publications
          </a>
          <a href="#projects" class="px-5 py-3 rounded-xl border border-slate-700 text-slate-200 hover:border-cyan-400 hover:text-cyan-300 transition-all">
            Explore Projects
          </a>
          <a href="https://orcid.org/0009-0006-5247-3629" target="_blank" rel="noopener noreferrer" class="px-5 py-3 rounded-xl border border-green-500/40 text-green-300 hover:bg-green-500/10 transition-all">
            ORCID Profile
          </a>
        </div>
      </section>

      <aside class="rounded-2xl border border-green-500/30 bg-black/70 p-5 font-mono text-sm shadow-[0_0_35px_rgba(34,197,94,0.10)]">
        <div class="flex items-center gap-2 mb-4">
          <span class="w-3 h-3 rounded-full bg-red-400"></span>
          <span class="w-3 h-3 rounded-full bg-yellow-400"></span>
          <span class="w-3 h-3 rounded-full bg-green-400"></span>
          <span class="ml-2 text-slate-500">research-terminal</span>
        </div>

        <p><span class="text-slate-500">luis@portfolio:~$</span> ./scan_profile --public</p>
        <p class="text-cyan-400 mt-2">&gt; Loading research identity...</p>
        <p class="text-green-400">&gt; Building dynamic publication layer...</p>
        <p class="text-purple-300">&gt; Checking ORCID + DOI metadata...</p>
        <p class="text-slate-300 mt-4"><span id="typewriter" class="font-bold text-white"></span><span class="animate-pulse">_</span></p>

        <div class="mt-5 rounded-xl border border-slate-800 bg-slate-950/70 p-4">
          <p class="text-slate-500 mb-2">live_status.log</p>
          <p id="terminal-status" class="text-cyan-300">Initializing scientific profile...</p>
        </div>
      </aside>
    </header>

    <section id="research" class="mt-24">
      <div class="flex flex-col md:flex-row md:items-end justify-between gap-4 border-b border-slate-800 pb-4 mb-10">
        <div>
          <p class="text-xs font-bold text-cyan-400 uppercase tracking-[0.3em] mb-2">Research Identity</p>
          <h2 class="text-3xl font-bold text-white">Research Focus</h2>
        </div>
        <p class="text-slate-500 max-w-2xl">
          A portfolio designed as a living academic profile: projects, publications, citation signals, and original links in one place.
        </p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-5">
        <article class="rounded-2xl border border-slate-800 bg-slate-900/50 p-6 hover:border-cyan-500/50 transition-all">
          <p class="text-cyan-400 font-mono text-sm mb-3">01</p>
          <h3 class="text-xl font-bold text-white mb-2">Context-Aware Emotion Recognition</h3>
          <p class="text-slate-400 text-sm leading-relaxed">Bayesian-style context fusion, scene priors, temporal smoothing, and real-time facial emotion analysis.</p>
        </article>

        <article class="rounded-2xl border border-slate-800 bg-slate-900/50 p-6 hover:border-blue-500/50 transition-all">
          <p class="text-blue-400 font-mono text-sm mb-3">02</p>
          <h3 class="text-xl font-bold text-white mb-2">Multimodal Transformers</h3>
          <p class="text-slate-400 text-sm leading-relaxed">Image/audio fusion, cross-attention, hardware-aware inference, and experimental emotion recognition pipelines.</p>
        </article>

        <article class="rounded-2xl border border-slate-800 bg-slate-900/50 p-6 hover:border-purple-500/50 transition-all">
          <p class="text-purple-400 font-mono text-sm mb-3">03</p>
          <h3 class="text-xl font-bold text-white mb-2">Embedded AI & Smart Sensing</h3>
          <p class="text-slate-400 text-sm leading-relaxed">IoT monitoring, environmental sensing, microcontrollers, edge devices, cloud dashboards, and real-time deployment.</p>
        </article>
      </div>
    </section>

    <section id="projects" class="mt-24">
      <div class="border-b border-slate-800 pb-4 mb-10">
        <p class="text-xs font-bold text-cyan-400 uppercase tracking-[0.3em] mb-2">Built Systems</p>
        <h2 class="text-3xl font-bold text-white">Featured Projects</h2>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
        <article class="group rounded-2xl border border-slate-800 bg-slate-900/50 overflow-hidden hover:border-cyan-500/50 transition-all flex flex-col">
          <div class="aspect-video w-full bg-black">
            <iframe
              class="w-full h-full"
              src="https://www.youtube.com/embed/DExxQR5RKNM"
              title="The Robot MARKUS demo"
              frameborder="0"
              allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
              allowfullscreen>
            </iframe>
          </div>
          <div class="p-6">
            <div class="flex items-center gap-2 mb-2">
              <h3 class="text-xl font-bold text-white">The Robot MARKUS</h3>
              <span class="px-2 py-0.5 text-[10px] font-bold uppercase rounded bg-yellow-500/20 text-yellow-300 border border-yellow-500/30">Awarded</span>
            </div>
            <p class="text-slate-400 text-sm leading-relaxed">Service robot project focused on customer-service optimization, embedded logic, sensing, and applied robotics.</p>
          </div>
        </article>

        <article class="group rounded-2xl border border-slate-800 bg-slate-900/50 overflow-hidden hover:border-cyan-500/50 transition-all flex flex-col">
          <div class="h-52 w-full bg-slate-800 relative flex justify-center items-center">
            <span class="absolute z-10 text-slate-500 font-mono text-sm">Upload: /public/saqms.jpg</span>
            <img src="/portafolio-luis/saqms.jpg" alt="SAQMS" class="absolute inset-0 w-full h-full object-cover z-20 grayscale group-hover:grayscale-0 transition-all duration-500 opacity-0 hover:opacity-100" onload="this.classList.remove('opacity-0'); this.previousElementSibling.style.display='none';" />
          </div>
          <div class="p-6">
            <div class="flex items-center gap-2 mb-2">
              <h3 class="text-xl font-bold text-white">SAQMS</h3>
              <span class="px-2 py-0.5 text-[10px] font-bold uppercase rounded bg-blue-500/20 text-blue-300 border border-blue-500/30">IEEE</span>
            </div>
            <p class="text-slate-400 text-sm leading-relaxed">Smart Air Quality Monitoring System with sensor acquisition, cloud connectivity, mobile access, and predictive analytics.</p>
          </div>
        </article>

        <article class="group rounded-2xl border border-slate-800 bg-slate-900/50 overflow-hidden hover:border-blue-500/50 transition-all flex flex-col">
          <div class="h-52 w-full bg-slate-800 relative flex justify-center items-center">
            <span class="absolute z-10 text-slate-500 font-mono text-sm">Upload: /public/ia-vision.jpg</span>
            <img src="/portafolio-luis/ia-vision.jpg" alt="Computer vision system" class="absolute inset-0 w-full h-full object-cover z-20 grayscale group-hover:grayscale-0 transition-all duration-500 opacity-0 hover:opacity-100" onload="this.classList.remove('opacity-0'); this.previousElementSibling.style.display='none';" />
          </div>
          <div class="p-6">
            <h3 class="text-xl font-bold text-white mb-2">Real-Time AI Integration</h3>
            <p class="text-slate-400 text-sm leading-relaxed">Deployment of computer-vision models on embedded platforms, integrating camera streams, control, and optimization.</p>
          </div>
        </article>

        <article class="group rounded-2xl border border-slate-800 bg-slate-900/50 overflow-hidden hover:border-purple-500/50 transition-all flex flex-col">
          <div class="h-52 w-full bg-slate-800 relative flex justify-center items-center">
            <span class="absolute z-10 text-slate-500 font-mono text-sm">Upload: /public/drone.jpg</span>
            <img src="/portafolio-luis/drone.jpg" alt="Drone tracking" class="absolute inset-0 w-full h-full object-cover z-20 grayscale group-hover:grayscale-0 transition-all duration-500 opacity-0 hover:opacity-100" onload="this.classList.remove('opacity-0'); this.previousElementSibling.style.display='none';" />
          </div>
          <div class="p-6">
            <h3 class="text-xl font-bold text-white mb-2">Drone Detection & Tracking</h3>
            <p class="text-slate-400 text-sm leading-relaxed">Real-time tracking experiments combining fixed cameras, zoom camera behavior, and visual control logic for long-range targets.</p>
          </div>
        </article>
      </div>
    </section>

    <section id="publications" class="mt-24">
      <div class="flex flex-col lg:flex-row lg:items-end justify-between gap-6 border-b border-slate-800 pb-4 mb-8">
        <div>
          <p class="text-xs font-bold text-green-400 uppercase tracking-[0.3em] mb-2">Live Academic Layer</p>
          <h2 class="text-3xl font-bold text-white">Scientific Publications</h2>
        </div>

        <div class="grid grid-cols-3 gap-3 w-full lg:w-auto">
          <div class="rounded-xl border border-slate-800 bg-slate-900/50 px-4 py-3 text-center">
            <p id="metric-publications" class="text-2xl font-bold text-white">—</p>
            <p class="text-[10px] uppercase tracking-widest text-slate-500">Indexed</p>
          </div>
          <div class="rounded-xl border border-slate-800 bg-slate-900/50 px-4 py-3 text-center">
            <p id="metric-citations" class="text-2xl font-bold text-white">—</p>
            <p class="text-[10px] uppercase tracking-widest text-slate-500">Citations</p>
          </div>
          <div class="rounded-xl border border-slate-800 bg-slate-900/50 px-4 py-3 text-center">
            <p id="metric-updated" class="text-sm font-mono text-cyan-300">Live</p>
            <p class="text-[10px] uppercase tracking-widest text-slate-500">Sync</p>
          </div>
        </div>
      </div>

      <p class="text-slate-500 text-sm mb-8 max-w-4xl">
        Publications below combine curated records with a dynamic ORCID layer. Citation indicators are retrieved through DOI-based metadata when available, so counts may differ from Google Scholar or publisher pages.
      </p>

      <div class="space-y-4 mb-10">
        <article class="p-6 rounded-xl border-2 border-blue-500/30 bg-blue-900/10 hover:bg-blue-900/20 transition-colors group">
          <div class="flex flex-col md:flex-row md:items-center justify-between gap-5">
            <div>
              <div class="flex flex-wrap items-center gap-3 mb-2">
                <span class="px-2 py-0.5 text-[10px] font-bold tracking-wider rounded bg-blue-500/20 text-blue-300 border border-blue-500/30">IEEE I&M MAGAZINE</span>
                <span class="text-sm font-mono text-slate-400">2026</span>
                <span id="citation-ieee-saqms" class="px-2 py-0.5 text-[10px] font-bold rounded bg-green-500/10 text-green-300 border border-green-500/20">Citations: —</span>
              </div>
              <h3 class="text-xl font-bold text-white group-hover:text-blue-300 transition-colors">Education in I&M: Smart Air Quality Monitoring System — A Student Project</h3>
              <p class="text-sm text-slate-400 mt-1">L. E. Arenas-Deseano, J. M. Ramirez-Cortes, P. Gomez-Gil</p>
              <p class="text-xs text-slate-500 mt-2">DOI: 10.1109/MIM.2026.11224834</p>
            </div>

            <div class="flex flex-wrap gap-2 md:justify-end">
              <a href="https://ieeexplore.ieee.org/document/11482784" target="_blank" rel="noopener noreferrer" class="px-4 py-2 text-sm font-medium rounded-lg bg-blue-600 hover:bg-blue-500 text-white transition-colors">
                IEEE Xplore
              </a>
              <a href="https://doi.org/10.1109/MIM.2026.11224834" target="_blank" rel="noopener noreferrer" class="px-4 py-2 text-sm font-medium rounded-lg border border-slate-700 text-slate-300 hover:border-cyan-400 hover:text-cyan-300 transition-colors">
                DOI
              </a>
            </div>
          </div>
        </article>

        <article class="p-6 rounded-xl border border-slate-800 bg-slate-900/40 hover:bg-slate-800/50 transition-colors">
          <div class="flex flex-col md:flex-row md:items-center justify-between gap-5">
            <div>
              <div class="flex flex-wrap items-center gap-3 mb-2">
                <span class="px-2 py-0.5 text-[10px] font-bold uppercase rounded bg-cyan-500/10 text-cyan-300 border border-cyan-500/30">Revista Latinoamericana · NOTICIEEEERO</span>
                <span class="text-sm font-mono text-slate-500">Magazine / Outreach</span>
              </div>
              <h3 class="text-xl font-bold text-slate-200">Revista Latinoamericana Publication</h3>
              <p class="text-sm text-slate-400 mt-1">Public-facing regional publication record linked to the original Calaméo issue.</p>
            </div>

            <a href="https://www.calameo.com/read/007124124de34efadd8c4" target="_blank" rel="noopener noreferrer" class="shrink-0 px-4 py-2 text-sm font-medium rounded-lg border border-cyan-500/40 text-cyan-300 hover:bg-cyan-500/10 transition-colors">
              Open NOTICIEEEERO
            </a>
          </div>
        </article>

        <article id="pending-accepted-paper" data-pending-title="context-aware emotion" class="p-6 rounded-xl border border-purple-500/30 bg-purple-500/10 hover:bg-purple-500/15 transition-colors">
          <div class="flex flex-col md:flex-row md:items-center justify-between gap-5">
            <div>
              <div class="flex flex-wrap items-center gap-3 mb-2">
                <span class="px-2 py-0.5 text-[10px] font-bold uppercase rounded bg-purple-500/20 text-purple-300 border border-purple-500/30">Accepted · In Production</span>
                <span class="text-sm font-mono text-slate-500">Paused until indexed</span>
              </div>
              <h3 class="text-xl font-bold text-slate-200">Context-Aware Emotion Classification</h3>
              <p class="text-sm text-slate-400 mt-1">Accepted work currently in the publication process. This card is intentionally separated from the dynamic ORCID history until the official record appears.</p>
            </div>
            <span class="shrink-0 px-4 py-2 text-sm font-medium rounded-lg border border-purple-500/40 text-purple-300">
              Awaiting official link
            </span>
          </div>
        </article>
      </div>

      <div class="flex flex-col md:flex-row md:items-center justify-between gap-4 border-t border-slate-800 pt-8 mb-5">
        <h3 class="text-xs font-bold text-slate-500 uppercase tracking-widest">Dynamic History via ORCID</h3>
        <div class="flex flex-wrap gap-4">
          <a href="https://ieeexplore.ieee.org/author/395576129474048" target="_blank" rel="noopener noreferrer" class="text-sm text-blue-400 hover:text-blue-300 transition-colors">
            IEEE Author Profile &rarr;
          </a>
          <a href="https://orcid.org/0009-0006-5247-3629" target="_blank" rel="noopener noreferrer" class="text-sm text-green-400 hover:text-green-300 transition-colors">
            Full ORCID Profile &rarr;
          </a>
        </div>
      </div>

      <div id="orcid-publications" class="space-y-4">
        <p class="text-slate-500 animate-pulse">Querying ORCID public record...</p>
      </div>
    </section>

    <section id="contact" class="mt-24 mb-20 p-10 rounded-3xl bg-gradient-to-br from-slate-900 to-black border border-slate-800 text-center relative overflow-hidden">
      <div class="absolute top-0 left-1/2 -translate-x-1/2 w-3/4 h-32 bg-cyan-500/20 blur-[100px]"></div>
      <h2 class="text-3xl font-bold text-white mb-4 relative z-10">Collaboration, Research & Technology Consulting</h2>
      <p class="text-slate-400 max-w-2xl mx-auto mb-8 relative z-10">
        Available for academic collaboration, technology projects, talks, consulting, and applied research in intelligent systems.
      </p>
      <div class="flex flex-wrap justify-center gap-3 relative z-10">
        <a href="mailto:wicholuiz29@gmail.com" class="inline-block px-7 py-3 rounded-xl bg-white text-black font-bold hover:bg-cyan-400 transition-all">
          Contact Me
        </a>
        <a href="https://github.com/WichoArenas" target="_blank" rel="noopener noreferrer" class="inline-block px-7 py-3 rounded-xl border border-slate-700 text-slate-200 hover:border-cyan-400 hover:text-cyan-300 transition-all">
          GitHub
        </a>
        <a href="https://www.linkedin.com/in/luis-eduardo-arenas-deseano-b97b8aa5/" target="_blank" rel="noopener noreferrer" class="inline-block px-7 py-3 rounded-xl border border-slate-700 text-slate-200 hover:border-cyan-400 hover:text-cyan-300 transition-all">
          LinkedIn
        </a>
      </div>
    </section>
  </main>
</Layout>

<style>
  @keyframes scan {
    0% { transform: translateY(-100%); }
    100% { transform: translateY(100%); }
  }
</style>

<script is:inline>
  const ORCID_ID = "0009-0006-5247-3629";
  const ORCID_URL = `https://pub.orcid.org/v3.0/${ORCID_ID}/works`;

  const manualCitationSources = [
    {
      key: "ieee-saqms",
      doi: "10.1109/MIM.2026.11224834",
      badgeId: "citation-ieee-saqms"
    }
  ];

  const typewriterPhrases = [
    "Computer Vision Researcher",
    "Multimodal AI Developer",
    "Embedded Intelligence Builder",
    "Context-Aware Emotion Recognition",
    "IoT + Smart Sensing Systems",
    "Real-Time AI on Hardware"
  ];

  const terminalStatusMessages = [
    "ORCID public record connected.",
    "DOI citation counter enabled when metadata exists.",
    "Accepted manuscripts remain paused until official indexing.",
    "IEEE, ORCID, DOI and magazine links are preserved.",
    "Portfolio running as a static GitHub Pages site."
  ];

  const escapeHTML = (value) => String(value ?? "")
    .replaceAll("&", "&amp;")
    .replaceAll("<", "&lt;")
    .replaceAll(">", "&gt;")
    .replaceAll('"', "&quot;")
    .replaceAll("'", "&#039;");

  const normalize = (value) => String(value ?? "").toLowerCase().replace(/\s+/g, " ").trim();

  function readCache(key, maxAgeMs = 12 * 60 * 60 * 1000) {
    try {
      const cached = JSON.parse(localStorage.getItem(key));
      if (!cached) return null;
      if (Date.now() - cached.timestamp > maxAgeMs) return null;
      return cached.value;
    } catch {
      return null;
    }
  }

  function writeCache(key, value) {
    try {
      localStorage.setItem(key, JSON.stringify({ timestamp: Date.now(), value }));
    } catch {
      // Cache is optional.
    }
  }

  async function fetchCitationCountByDoi(doi) {
    if (!doi) return null;

    const normalizedDoi = doi.trim().toLowerCase();
    const cacheKey = `portfolio:citation:${normalizedDoi}`;
    const cached = readCache(cacheKey);
    if (typeof cached === "number") return cached;

    // Primary source: Semantic Scholar paper endpoint by DOI.
    try {
      const paperId = encodeURIComponent(`DOI:${normalizedDoi}`);
      const response = await fetch(`https://api.semanticscholar.org/graph/v1/paper/${paperId}?fields=citationCount,title,year,url`);
      if (response.ok) {
        const data = await response.json();
        if (typeof data.citationCount === "number") {
          writeCache(cacheKey, data.citationCount);
          return data.citationCount;
        }
      }
    } catch {
      // Continue to Crossref fallback.
    }

    // Fallback source: Crossref "is-referenced-by-count" when available.
    try {
      const response = await fetch(`https://api.crossref.org/works/${encodeURIComponent(normalizedDoi)}`);
      if (response.ok) {
        const data = await response.json();
        const count = data?.message?.["is-referenced-by-count"];
        if (typeof count === "number") {
          writeCache(cacheKey, count);
          return count;
        }
      }
    } catch {
      // Citation count is optional.
    }

    return null;
  }

  function extractExternalId(summary, preferredType) {
    const ids = summary?.["external-ids"]?.["external-id"] ?? [];
    const match = ids.find((item) => normalize(item?.["external-id-type"]) === preferredType);
    return match ?? null;
  }

  function externalLinkFromSummary(summary) {
    const directUrl = summary?.url?.value;
    if (directUrl) return directUrl;

    const doi = extractExternalId(summary, "doi");
    const doiUrl = doi?.["external-id-url"]?.value;
    if (doiUrl) return doiUrl;

    const doiValue = doi?.["external-id-value"];
    if (doiValue) return `https://doi.org/${doiValue}`;

    const anyUrl = (summary?.["external-ids"]?.["external-id"] ?? [])
      .find((item) => item?.["external-id-url"]?.value)?.["external-id-url"]?.value;

    return anyUrl || null;
  }

  function renderOrcidCard({ title, year, type, link, doi, citationCount }) {
    const safeTitle = escapeHTML(title);
    const safeType = escapeHTML(type || "WORK");
    const safeYear = escapeHTML(year || "N/A");
    const safeLink = link ? escapeHTML(link) : null;
    const safeDoi = doi ? escapeHTML(doi) : null;

    return `
      <article class="p-5 rounded-xl border border-slate-800 bg-slate-900/40 hover:bg-slate-800/50 transition-colors group">
        <div class="flex flex-col md:flex-row md:items-center justify-between gap-4">
          <div>
            <div class="flex flex-wrap items-center gap-3 mb-1">
              <span class="px-2 py-0.5 text-[10px] font-bold tracking-wider rounded bg-green-500/10 text-green-300 border border-green-500/20">${safeType}</span>
              <span class="text-sm font-mono text-slate-500">${safeYear}</span>
              <span class="px-2 py-0.5 text-[10px] font-bold rounded bg-slate-800 text-slate-300 border border-slate-700">Citations: ${citationCount ?? "—"}</span>
            </div>
            <h3 class="text-lg font-bold text-slate-200 group-hover:text-cyan-300 transition-colors">${safeTitle}</h3>
            ${safeDoi ? `<p class="text-xs text-slate-500 mt-2">DOI: ${safeDoi}</p>` : ""}
          </div>
          ${safeLink ? `<a href="${safeLink}" target="_blank" rel="noopener noreferrer" class="shrink-0 px-4 py-2 text-sm rounded-lg border border-slate-700 hover:border-cyan-500 text-slate-300 hover:text-cyan-300 transition-colors">Open record &rarr;</a>` : ""}
        </div>
      </article>
    `;
  }

  async function updateManualCitationBadges() {
    let total = 0;

    for (const item of manualCitationSources) {
      const count = await fetchCitationCountByDoi(item.doi);
      const badge = document.getElementById(item.badgeId);
      if (badge) badge.textContent = `Citations: ${count ?? "—"}`;
      if (typeof count === "number") total += count;
    }

    return total;
  }

  async function fetchOrcidWorks() {
    const container = document.getElementById("orcid-publications");
    const publicationMetric = document.getElementById("metric-publications");
    const citationMetric = document.getElementById("metric-citations");
    const updatedMetric = document.getElementById("metric-updated");
    const pendingPaper = document.getElementById("pending-accepted-paper");

    if (!container) return;

    let totalCitations = await updateManualCitationBadges();
    let renderedCount = 2; // IEEE + Revista Latinoamericana curated entries.

    try {
      const response = await fetch(ORCID_URL, { headers: { "Accept": "application/json" } });
      if (!response.ok) throw new Error("Unable to connect to ORCID public API.");

      const data = await response.json();
      const groups = data?.group ?? [];

      const renderedTitles = new Set([
        normalize("Education in I&M: Smart Air Quality Monitoring System — A Student Project"),
        normalize("Smart Air Quality Monitoring System - a Student Project"),
        normalize("Revista Latinoamericana Publication")
      ]);

      const cards = [];

      for (const workGroup of groups) {
        const summary = workGroup?.["work-summary"]?.[0];
        if (!summary?.title?.title?.value) continue;

        const title = summary.title.title.value;
        const normalizedTitle = normalize(title);

        if ([...renderedTitles].some((existing) => normalizedTitle.includes(existing) || existing.includes(normalizedTitle))) {
          continue;
        }

        if (normalizedTitle.includes("context-aware emotion") && pendingPaper) {
          pendingPaper.style.display = "none";
        }

        const year = summary?.["publication-date"]?.year?.value ?? "N/A";
        const type = String(summary?.type ?? "work").replaceAll("_", " ").toUpperCase();
        const link = externalLinkFromSummary(summary);
        const doi = extractExternalId(summary, "doi")?.["external-id-value"] ?? null;
        const citationCount = await fetchCitationCountByDoi(doi);

        if (typeof citationCount === "number") totalCitations += citationCount;
        renderedCount += 1;

        cards.push(renderOrcidCard({ title, year, type, link, doi, citationCount }));
      }

      if (cards.length === 0) {
        container.innerHTML = `
          <p class="text-slate-500">
            No additional ORCID works were found beyond the curated entries, or ORCID is still indexing the records.
          </p>
        `;
      } else {
        container.innerHTML = cards.join("");
      }

      if (publicationMetric) publicationMetric.textContent = String(renderedCount);
      if (citationMetric) citationMetric.textContent = totalCitations ? String(totalCitations) : "—";
      if (updatedMetric) updatedMetric.textContent = new Date().toLocaleDateString(undefined, { month: "short", day: "numeric" });
    } catch (error) {
      console.error(error);
      container.innerHTML = `
        <p class="text-red-400">
          Dynamic ORCID loading failed. Please use the direct ORCID and IEEE links above.
        </p>
      `;
      if (publicationMetric) publicationMetric.textContent = String(renderedCount);
      if (citationMetric) citationMetric.textContent = totalCitations ? String(totalCitations) : "—";
    }
  }

  function startTypewriter() {
    const target = document.getElementById("typewriter");
    if (!target) return;

    let phraseIndex = 0;
    let charIndex = 0;
    let deleting = false;

    function tick() {
      const phrase = typewriterPhrases[phraseIndex];

      if (deleting) {
        target.textContent = phrase.substring(0, charIndex - 1);
        charIndex -= 1;
      } else {
        target.textContent = phrase.substring(0, charIndex + 1);
        charIndex += 1;
      }

      let speed = deleting ? 30 : 65;

      if (!deleting && charIndex === phrase.length) {
        speed = 1700;
        deleting = true;
      } else if (deleting && charIndex === 0) {
        deleting = false;
        phraseIndex = (phraseIndex + 1) % typewriterPhrases.length;
        speed = 350;
      }

      setTimeout(tick, speed);
    }

    tick();
  }

  function startTerminalStatus() {
    const target = document.getElementById("terminal-status");
    if (!target) return;

    let index = 0;
    setInterval(() => {
      index = (index + 1) % terminalStatusMessages.length;
      target.textContent = terminalStatusMessages[index];
    }, 2600);
  }

  function startNeuralBackground() {
    const canvas = document.getElementById("neural-bg");
    if (!canvas) return;

    const ctx = canvas.getContext("2d");
    if (!ctx) return;

    function resize() {
      canvas.width = window.innerWidth;
      canvas.height = window.innerHeight;
    }

    resize();

    const particles = Array.from({ length: 70 }, () => ({
      x: Math.random() * canvas.width,
      y: Math.random() * canvas.height,
      vx: (Math.random() - 0.5) * 0.45,
      vy: (Math.random() - 0.5) * 0.45,
      radius: Math.random() * 2 + 0.5
    }));

    function draw() {
      ctx.clearRect(0, 0, canvas.width, canvas.height);

      for (let i = 0; i < particles.length; i++) {
        const particle = particles[i];

        particle.x += particle.vx;
        particle.y += particle.vy;

        if (particle.x < 0 || particle.x > canvas.width) particle.vx *= -1;
        if (particle.y < 0 || particle.y > canvas.height) particle.vy *= -1;

        ctx.beginPath();
        ctx.arc(particle.x, particle.y, particle.radius, 0, Math.PI * 2);
        ctx.fillStyle = "#06b6d4";
        ctx.fill();

        for (let j = i + 1; j < particles.length; j++) {
          const other = particles[j];
          const dx = particle.x - other.x;
          const dy = particle.y - other.y;
          const distance = Math.sqrt(dx * dx + dy * dy);

          if (distance < 140) {
            ctx.beginPath();
            ctx.strokeStyle = `rgba(6, 182, 212, ${1 - distance / 140})`;
            ctx.lineWidth = 0.45;
            ctx.moveTo(particle.x, particle.y);
            ctx.lineTo(other.x, other.y);
            ctx.stroke();
          }
        }
      }

      requestAnimationFrame(draw);
    }

    window.addEventListener("resize", resize);
    draw();
  }

  document.addEventListener("DOMContentLoaded", () => {
    startTypewriter();
    startTerminalStatus();
    startNeuralBackground();
    fetchOrcidWorks();
  });
</script>
